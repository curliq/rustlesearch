version: '3.7'

services:
  rustlesearch_dev:
    image: node:12
    volumes:
      - .:/node_app
    working_dir: /node_app
    command: echo 1
    environment:
      - VIRTUAL_HOST=${DOMAIN}
      - VIRTUAL_PORT=${APP_PORT}
      - LETSENCRYPT_HOST=${DOMAIN}
    expose:
      - ${APP_PORT}
    depends_on:
      - redis

